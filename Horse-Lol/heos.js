document.write("I love horses!");
document.write("write me a topic about horses!")

var horses = 0;

//typing shit lol:

document.onkeypress = function(a) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(b) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(c) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(d) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(e) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(f) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(g) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(h) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(i) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = "i";
  document.body.appendChild(horseSpan);
  horses += "i";
  // use e.keyCode if needed
};
document.onkeypress = function(j) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(k) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(l) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(m) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(n) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};document.onkeypress = function(o) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};document.onkeypress = function(p) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(q) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(r) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(s) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(t) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(u) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(v) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(w) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(x) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(y) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};
document.onkeypress = function(z) {
  var horseSpan = document.createElement("span");
  horseSpan.textContent = " horse";
  document.body.appendChild(horseSpan);
  horses += " horse";
  // use e.keyCode if needed
};